import { FontSizeControlPipe } from './font-size-control.pipe';

describe('FontSizeControlPipe', () => {
  it('create an instance', () => {
    const pipe = new FontSizeControlPipe();
    expect(pipe).toBeTruthy();
  });
});
